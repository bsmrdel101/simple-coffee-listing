/** @type {import('next').NextConfig} */
const nextConfig = {
  crossOrigin: 'anonymous',
  images: {
    domains: ['csyxkpbavpcrhwqhcpyy.supabase.co', 'localhost'],
  },
};

export default nextConfig;
